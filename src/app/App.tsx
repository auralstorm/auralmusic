function App() {
  return (
    <main className="app-shell">
      <section className="app-card">
        <p className="app-eyebrow">AuralMusic</p>
        <h1>Vite + React engineering baseline</h1>
        <p className="app-copy">
          The project is ready for feature work after tooling setup and Git workflow
          validation.
        </p>
      </section>
    </main>
  );
}

export default App;
